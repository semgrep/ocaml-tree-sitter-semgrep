name: Test languages

# Per-language grammar build/test matrix. Replaces the former CircleCI
# `test-language` job (.circleci/config.yml, removed in #627).
#
# For each language from lang/scripts/list-languages, runs
# `./test-lang <lang>`. All failures are fatal.

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

# One matrix run per ref; newer pushes cancel the previous (saves minutes).
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  enumerate:
    name: Enumerate languages
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      languages: ${{ steps.list.outputs.languages }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: list
        run: |
          set -euo pipefail
          echo "languages=$(lang/scripts/list-languages | jq -Rnc '[inputs]')" >> "$GITHUB_OUTPUT"

  test-language:
    name: test-lang ${{ matrix.language }}
    needs: enumerate
    # c-sharp-pro OOMs on a standard ~16GB runner during 'tree-sitter generate',
    # so it gets an org larger runner. Label is singular: ubuntu-latest-8-core.
    runs-on: ${{ matrix.language == 'c-sharp-pro' && fromJSON('{"group":"Default Larger Runners","labels":"ubuntu-latest-8-core"}') || 'ubuntu-latest' }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      max-parallel: 8
      matrix:
        language: ${{ fromJSON(needs.enumerate.outputs.languages) }}
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      # Install OCaml + opam directly (the old returntocorp/ocaml CircleCI
      # image is no longer pullable). Our fork is used for save-opam-post-run.
      # Saving in the post step captures the switch after 'make setup' has populated it.
      # cache-prefix carries the dependency set, because the action's own cache
      # key does not: without it a dependency change would keep restoring the
      # stale switch forever.
      #
      # Keep these four inputs identical to ci.yml and propose-grammar-updates.
      - uses: semgrep/setup-ocaml@a739c5405d73c42ef15a9dc995efc0f87396cc36
        with:
          cache-prefix: v5-${{ hashFiles('core/*.opam') }}
          ocaml-compiler: ocaml-variants.5.3.0+options,ocaml-option-flambda
          opam-pin: false
          save-opam-post-run: true
      - uses: ./.github/actions/build-core
      - name: test-lang ${{ matrix.language }}
        run: |
          set -euo pipefail
          eval "$(opam env)"
          cd lang
          ./test-lang "${{ matrix.language }}"

  # Single required check for branch protection (matrix expands to many jobs).
  test-languages:
    name: Test languages
    needs: test-language
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: All languages passed
        run: |
          result="${{ needs.test-language.result }}"
          echo "matrix result: $result"
          test "$result" = "success"
